#include <stdio.h> 

int main () {
    struct hr {
        int hr, min, seg;
    };

    struct data {
        int dia, mes, ano;
    };

    struct.afazer {
        struct data esta_data;
        struct horario este_hr;
        char afazer [100];
    };

    return 0;
}
